# Migrations

Filenames use the exact `<version>_<name>` recorded in
`supabase_migrations.schema_migrations` on the live project
(`ghwpmtmbqtchsrnagoir`), pulled directly from that table so this directory
matches applied history exactly.

Applied on the live project: `001` through `014b` (14 migrations, ending
`20260829015429_014b_fix_resource_files_entitlement.sql`).

**Not yet applied** — drafted for review, per instruction not to apply
further migrations to production before sign-off:
`20260829120000_015_resource_type_publish_targets.sql`.
